% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_daily_profiles}
\alias{plot_daily_profiles}
\title{Plot daily soil moisture profiles (t vs t-1) with optional rain/ET titles}
\usage{
plot_daily_profiles(
  res,
  depths,
  dz,
  rain = NULL,
  ET0 = NULL,
  file = NULL,
  days = NULL,
  xlim = c(0, 0.4),
  style = c("segments", "rect"),
  add_areas = FALSE,
  alpha = 0.3,
  main_prefix = "Daily profile"
)
}
\arguments{
\item{res}{List returned by `musec_simulate()`. Must contain
`res$series$theta_mm` [layers x steps] and `res$series$evap_mm` [steps].}

\item{depths}{Numeric vector of layer centers [m], length = n_layers.}

\item{dz}{Numeric vector of layer thicknesses [m], length = n_layers.}

\item{rain}{Optional numeric vector [steps] with rainfall per time step [mm].}

\item{ET0}{Optional numeric vector [steps] with ET0 per time step [mm].}

\item{file}{Optional PDF file path. If non-NULL, plots are saved into a PDF.
If NULL, plots are sent to the current device.}

\item{days}{Optional integer vector of day indices to plot. Defaults to 2:n_days
(day 1 is skipped because it needs t-1 to compare).}

\item{xlim}{X-axis limits for theta (default c(0, 0.40)).}

\item{style}{"segments" (classic stair outline) or "rect" (filled bars + spine).}

\item{add_areas}{Logical. If TRUE, when daily rain==0, highlight per-layer water loss
area between t and t-1 (semi-transparent).}

\item{alpha}{Alpha for filled areas when add_areas=TRUE and style="rect".}

\item{main_prefix}{Character prefix used in plot titles.}
}
\value{
(Invisibly) a list with meta info (n_days, plotted days). Primarily called for its side-effect (plots).
}
\description{
Plot daily soil moisture profiles (t vs t-1) with optional rain/ET titles
}
